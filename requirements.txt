pandas>=2.0
numpy>=1.24
requests>=2.28
beautifulsoup4>=4.12
histdata>=1.1
tzdata>=2024.1
scipy>=1.11          # ATR percentile rank + swing detection
matplotlib>=3.8      # equity curve chart

# Phase 2 (live MT5 bridge — install only on Windows VPS):
# MetaTrader5>=5.0.45
